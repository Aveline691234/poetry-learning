# 2026-05-02 工作日志

## 完成：AI古诗小老师 Vercel 部署改造

### 做了什么

1. **改写 `api/chat.py`** - 从无效的 vercel 包装器改为标准 Python `http.server` 格式（Vercel 官方要求）
2. **新建 `api/progress.py`** - Supabase 版进度存储 API，支持 save/load/all_progress（替代原本地文件存储）
3. **修复前端硬编码** - `teacher.html`, `progress.html`, `chat.html`, `recite.html` 中的 `localhost:8000` 改为相对路径或 `window.location.origin`
4. **更新 `vercel.json`** - 清理配置，添加 `/api/progress` 路由
5. **清理 `requirements.txt`** - 移除无用的 vercel 包，只保留 `requests`
6. **创建部署包** - `F:\trae\tiku\deploy-poetry\`，干净的 Git 仓库，已提交

### 部署包位置
`F:\trae\tiku\deploy-poetry\`

### 部署方案
- 平台：Vercel
- 前端：静态 HTML（Vercel Static）
- AI API：`/api/chat` → DeepSeek API
- 进度存储：`/api/progress` → Supabase PostgreSQL
- 用户选择：Vercel + Supabase

### 待用户操作（手动步骤）
1. GitHub 创建空仓库
2. `git remote add origin` + `git push`
3. Vercel 导入项目
4. 配置 3 个环境变量：DEEPSEEK_API_KEY, SUPABASE_URL, SUPABASE_ANON_KEY
5. Supabase 建表（SQL 已提供）
6. Redeploy

### 项目备注
- 原项目在 `F:\trae\tiku\poetry-learning\`，父目录 `F:\trae\tiku\` 是一个混合的 Git 仓库
- 部署用独立的干净目录 `deploy-poetry\`
- `corpus/poem_data.json` 是古诗数据，需要保留
- `assets/`, `css/`, `js/` 目录为空（资源都用 CDN），保留结构
